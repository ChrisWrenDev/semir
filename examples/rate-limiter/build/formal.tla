---- MODULE APIRateLimiter ----
\* Generated by SEMIR Formal Verification Projection
\* Semantic IDs are traceable to the source model.
\* Do not edit manually; regenerate from the semantic model.

EXTENDS Naturals, FiniteSets, TLC

VARIABLE reservationState, clock

StateType == {
  BucketReady, \* semantic: sem://ratelimit/state/bucket-ready
  RequestAllowed, \* semantic: sem://ratelimit/state/request-allowed
  BucketEmpty, \* semantic: sem://ratelimit/state/bucket-empty
  RequestRejected, \* semantic: sem://ratelimit/state/request-rejected
}

\* Actions derived from SEMIR assertion graph

RefillTokens ==
  /\ reservationState \in {StateType}
  /\ reservationState' \in {BucketReady}
  /\ clock' = clock + 1
  \* semantic: sem://ratelimit/action/refill-tokens

AllowRequest ==
  /\ reservationState \in {StateType}
  /\ reservationState' \in {RequestAllowed}
  /\ clock' = clock + 1
  \* semantic: sem://ratelimit/action/allow-request

RejectRequest ==
  /\ reservationState \in {StateType}
  /\ reservationState' \in {RequestRejected}
  /\ clock' = clock + 1
  \* semantic: sem://ratelimit/action/reject-request

ResetBucket ==
  /\ reservationState \in {StateType}
  /\ reservationState' \in {BucketReady}
  /\ clock' = clock + 1
  \* semantic: sem://ratelimit/action/reset-bucket

\* Properties derived from SEMIR invariants

TokensRefilledEventTokensRefilledEventForbids ==
  [](TokensRefilledEvent => ~TokensRefilledEvent)
  \* semantic: sem://ratelimit/assertion/refill-forbids-double-count

BucketEmptyRequestAllowedForbids ==
  [](BucketEmpty => ~RequestAllowed)
  \* semantic: sem://ratelimit/assertion/bucket-empty-forbids-request-allowed

\* Temporal constraints derived from SEMIR assertions

ObservedWithin1s ==
  \* Temporal constraint: observable within 1s
  \* semantic: sem://ratelimit/assertion/refill-observable-within-1s

ObservedWithin2ms ==
  \* Temporal constraint: observable within 2ms
  \* semantic: sem://ratelimit/assertion/decision-latency-p99

Init ==
  reservationState \in {BucketReady, RequestAllowed, BucketEmpty, RequestRejected}
  /\ clock = 0

Next ==
  RefillTokens
  \/
  AllowRequest
  \/
  RejectRequest
  \/
  ResetBucket

Spec == Init /\ [][Next]_<<reservationState, clock>>

PROPERTY TokensRefilledEventTokensRefilledEventForbids
     ∧ BucketEmptyRequestAllowedForbids
====